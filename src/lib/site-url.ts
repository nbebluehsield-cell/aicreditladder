/** Canonical site origin — use for sitemap, JSON-LD, robots. Override in prod via NEXT_PUBLIC_SITE_URL if needed. */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://aicreditladder.com";
